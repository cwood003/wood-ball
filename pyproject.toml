[project]
name = "wood-ball"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cole", email = "cwood0107@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "dbt-core>=1.9.1",
    "dbt-duckdb>=1.9.1",
    "duckdb>=1.1.3",
    "great-tables>=0.15.0",
    "matplotlib>=3.10.0",
    "nba-api>=1.6.1",
    "plotly>=5.24.1",
    "polars>=1.17.1",
    "pyarrow>=18.1.0",
    "typer>=0.15.1",
    "whenever>=0.6.16",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
wood-ball = "wood_ball.cli.cli:app"

[dependency-groups]
dev = [
    "harlequin>=1.25.2",
    "marimo>=0.10.4",
    "nba-on-court>=0.2.1",
    "wat-inspector>=0.4.3",
]
